{
  "name": "novelcraft-roaming",
  "version": "0.1.0",
  "description": "Fragment-based creative writing assistant built with Electrobun and SQLite",
  "scripts": {
    "start": "bun run build:dev && electrobun dev",
    "build:dev": "bun install && electrobun build",
    "dev": "electrobun dev",
    "build": "electrobun build"
  },
  "dependencies": {
    "@preact/signals": "^2.9.4",
    "electrobun": "latest",
    "eta": "^3.5.0",
    "htm": "^3.1.1",
    "js-tiktoken": "^1.0.0",
    "preact": "^10.19.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
